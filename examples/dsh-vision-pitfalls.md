# 视觉插件兼容性检查

宿主版本、附件格式和模型输入能力需要一起验证。切换视觉处理方式时，旧会话中的图片块可能与新路径不兼容，可使用新测试会话确认差异。

升级宿主后重新检查插件入口与附件处理。自定义兼容补丁应记录适用版本，并通过对应维护流程重放。

模型合法返回空数组时应保留该结果，脚本输出字段则需要符合工具 schema。图片读取应经过路径围栏，配置路径的默认值应来自运行环境。

修改插件后先做语法检查与模拟测试，再验证真实附件。重启只影响对应宿主实例，并等待原监听释放后再启动。
